'use client'
import {FormEvent,useState} from 'react'
import {useRouter} from 'next/navigation'
export default function LoginForm(){const router=useRouter();const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});setBusy(false);if(!r.ok){setError('E-Mail oder Passwort ist falsch.');return}router.push('/');router.refresh()}return <form className="loginForm" onSubmit={submit}><label>E-Mail<input name="email" type="email" defaultValue="admin@lupenrein.local" required/></label><label>Passwort<input name="password" type="password" defaultValue="lupenrein-demo" required/></label>{error&&<p className="errorText">{error}</p>}<button className="primary wide" disabled={busy}>{busy?'Anmeldung …':'Anmelden'}</button></form>}
