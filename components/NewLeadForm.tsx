'use client'
import {FormEvent,useState} from 'react'
import {useRouter} from 'next/navigation'
export default function NewLeadForm(){const router=useRouter();const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);const body=Object.fromEntries(f.entries());const r=await fetch('/api/leads',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});setBusy(false);if(!r.ok){setError('Lead konnte nicht gespeichert werden.');return}const lead=await r.json();router.push(`/leads/${lead.id}`);router.refresh()}return <form className="leadForm" onSubmit={submit}><div className="formGrid"><label>Unternehmen *<input name="company" required/></label><label>Ort *<input name="city" defaultValue="Berlin" required/></label><label>Branche *<select name="sector" defaultValue="Büro"><option>Büro</option><option>Arztpraxis</option><option>Hotel</option><option>Schule</option><option>Hausverwaltung</option><option>Industrie</option><option>Baustelle</option></select></label><label>Ansprechpartner<input name="contact"/></label><label>E-Mail<input name="email" type="email"/></label><label>Telefon<input name="phone"/></label><label>Website<input name="website" placeholder="https://…"/></label><label>Adresse<input name="address"/></label></div><div className="aiHint"><b>KI-Vorqualifizierung</b><span>Beim Speichern werden Score, Potenzial und passende LUPENREIN-Leistung automatisch vorgeschlagen.</span></div>{error&&<p className="errorText">{error}</p>}<div className="formActions"><button type="button" className="secondary" onClick={()=>router.back()}>Abbrechen</button><button className="primary" disabled={busy}>{busy?'Analysiere …':'Lead speichern & bewerten'}</button></div></form>}
