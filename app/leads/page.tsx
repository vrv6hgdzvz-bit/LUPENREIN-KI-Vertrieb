import Link from 'next/link'
import {getLeads} from '@/lib/store'
export default async function Leads(){const leads=await getLeads();return <><div className="pageHead"><div><span className="eyebrow">CRM</span><h2>Leads</h2><p>KI-bewertete Unternehmen aus Berlin + 50 km.</p></div><Link href="/leads/new" className="primary">+ Lead erfassen</Link></div><div className="filters"><button>Alle Branchen</button><button>Score ≥ 80</button><button>Berlin + 50 km</button><button>Alle Status</button><input placeholder="Unternehmen suchen …"/></div><section className="panel"><div className="leadTable full"><div className="tableHead"><span>Unternehmen</span><span>Branche</span><span>Status</span><span>Empfehlung</span><span>Score</span></div>{leads.map(l=><Link className="tableRow" href={`/leads/${l.id}`} key={l.id}><span><b>{l.company}</b><small>{l.city}</small></span><span>{l.sector}</span><span><em className="pill">{l.status}</em></span><span>{l.service}</span><span className="score">{l.score}</span></Link>)}</div></section></>}
